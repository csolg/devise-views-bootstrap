require 'spec_helper'

describe Devise::Views::Bootstrap do
  it 'has a version number' do
    expect(Devise::Views::Bootstrap::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
